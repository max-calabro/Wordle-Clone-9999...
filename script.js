import { WORDS } from './words.js'

let solution = WORDS[Math.floor(Math.random() * WORDS.length)]
console.log(solution)

const initGameBoard = () => {
  for (let i = 0; i < 6; i++) {
    //make a new row
    for (let j = 0; j < 5; j++) {
      //make 5 containers in the row
    }
  }
}
